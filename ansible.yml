- name: INstall Nginx
  hosts: WEB
  become: true
  tasks:
    - name: Installl Nginx
      ansible.builtin.yum:
        name: httpd
        state: latest

    - name: Start Nginx
      ansible.builtin.systemd:
        name: Nginx
        state: started
        enabled: yes